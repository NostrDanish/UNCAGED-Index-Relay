{
  "scripts": {
    "dev": "deno serve -A --env-file --watch src/server.ts",
    "start": "deno serve -A --env-file src/server.ts",
    "test": "deno test -A --env-file=.env.test"
  },
  "dependencies": {
    "@nostrify/nostrify": "^0.49.0",
    "@opensearch-project/opensearch": "^3.5.1",
    "hono": "^4.11.5",
    "nostr-tools": "^2.20.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.0.10"
  }
}
